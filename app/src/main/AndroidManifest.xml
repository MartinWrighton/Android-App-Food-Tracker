<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppLayouts"
        tools:targetApi="31">
        <activity
            android:name=".add_hydration"
            android:exported="false"
            android:label="@string/title_activity_add_hydration"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppLayouts" />
        <activity
            android:name=".calendar"
            android:exported="false"
            android:label="@string/title_activity_calendar"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppLayouts" />
        <activity
            android:name=".add_nutrition"
            android:exported="false"
            android:label="@string/title_activity_add_nutrition"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppLayouts" />
        <activity
            android:name=".Profile"
            android:exported="false"
            android:label="@string/title_activity_profile"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppLayouts" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppLayouts">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".notification" />

        <provider
            android:name=".myProvider"
            android:authorities="com.example.applayouts.provider" />
    </application>

</manifest>